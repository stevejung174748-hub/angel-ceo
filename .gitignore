# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.lnk

# Secrets & credentials (never commit)
.env
.env.*
*.pem
*.key
*.p12
credentials.json
secrets.json
.npmrc

# Local-only docs (personal emails, paths, account names)
GIT_ACCOUNTS.md
GIT_ACCOUNTS.local.md
*.local.md
*.secret.md

# SSH keys (if ever placed in project by mistake)
id_rsa
id_ed25519*
!id_ed25519*.pub

# Local preview
.serve/

# Legacy / sibling project paths (must not enter this repo)
../jungpm-nightlife-guide/
jungpm-nightlife-guide/
angelie-night/
_cleanup_jungpm_old/
